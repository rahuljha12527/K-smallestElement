#include<bits/stdc++.h>
vector<int> kSmallest(int *input, int n, int k) {
	// Write your code here
  sort(input,input+n);
    vector<int> v;
    for(int i=0;i<k;i++){
        v.push_back(input[i]);
    }
    
    return v;
    
    
}
